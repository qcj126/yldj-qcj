表：serve、serve_item、serve_type、region。
serve表结构：id, serve_item_id, region_id, city_code, sale_status, price, is_hot, hot_time_stamp, create_time, update_time, create_by, update_by, status。
serve_item表结构：id, code, serve_type_id, name, serve_item_icon, img, unit, description, detail_img, reference_price, sort_num, active_status, create_time, update_time, create_by, update_by。
serve_type表结构：id, code, name, serve_type_icon, img, sort_num, active_status, create_time, update_time, create_by, update_by。
region表结构：id, city_code, name, manager_name, manager_phone, active_status, sort_num, create_time, update_time, create_by, update_by。

表之间的字段联系：
    serve与region：region_id = id。
    serve与serve_item：serve_item_id = id。
    serve_item与serve_type：serve_type_id = id。

业务需求：
    1：使用PutMapping，接口路径：PUT/foundations/operation/serve/onSale/{id}；
    2：首先判断sale_status是草稿还是下架状态，若不是草稿或下架状态则不允许上架，直接抛出异常即可；
    3：若是草稿或下架状态，在查看serve_item中的active_status是否为启用状态，若不是启用状态则不允许上架，直接抛出异常即可；
    4：若是草稿或下架状态，并且active_status为启用状态，则允许上架。
备注：
    1：sale_status草稿状态为0；
    2：sale_status下架状态为1；
    3：sale_status上架状态为2；
    4：active_status启用状态为2；
    5：active_status禁用状态为1。
